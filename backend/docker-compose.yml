version: '3'
services:
  crm-server:
    build:
      dockerfile: Dockerfile
      context: ./
    container_name: crm-server
    ports:
      - '5000:5000'
    restart: always
    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env
